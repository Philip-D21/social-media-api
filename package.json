{
  "name": "social-media-api",
  "version": "1.0.0",
  "description": "GET / unprotected routes  => home route to display all posts content\r GET /login (unprotected) ==> authenticate a new user\r GET /logout (unprotected) ==> logout a user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.5.0",
    "mongoose": "^7.2.1"
  }
}
